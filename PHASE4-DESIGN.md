# Phase 4 設計方針 — パズル統一理論 × ゲーム統一理論のAgent基盤再構築

## 核心的洞察

現在のパズルエンジン（puzzle.ts）とゲームエンジン（game.ts）は、v0.3時代に独立して設計された。
v0.5のAgent基盤（EventBus + Entity Agent + Mediator）の上に再構築すると、**パズルとゲームが同一の抽象**で記述できる。

```
パズル = 協調的マルチエージェントシステム（全セルが共通目標に向かう）
ゲーム = 競争的マルチエージェントシステム（プレイヤーが対立目標を持つ）
```

違いは Agent の `behavior` だけ:

| 領域 | Agent の behavior | Mediator の戦略 |
|------|-------------------|----------------|
| パズル | cooperative | cooperative（協調） |
| ゲーム | competitive | priority / mediator |
| ハイブリッド（協力ゲーム等） | mixed | sequential |

---

## 現状のアーキテクチャ（v0.5時点）

### puzzle.ts（905行）
- `PuzzleCell` — 値 + 候補リスト + 消去履歴
- `PuzzleSpace` — グリッド + 制約グループ + 伝播履歴
- 手続き的制約伝播: `propagateStep()` → `propagateNakedPair()` → `solvePuzzle()`
- セル間の連携は関数の中でハードコードされている

### game.ts（743行）
- `GameSpace` — 状態 + ルール + 戦略
- `GameState` — 盤面 + 手番 + 棋譜
- Minimax 探索がモノリシックに実装
- プレイヤーは概念的存在で、Agent として実体化されていない

### entity-agent.ts（1,153行）
- `ReiAgent` — perceive → decide → act サイクル
- 5つの behavior パターン
- `AgentRegistry` — spawn / destroy / 検索

### mediator.ts（770行）
- `ReiMediator` — 並行実行 + 競合解決
- 収束検出付きラウンドベース実行

---

## 再構築の設計

### A. パズル → Agent化

**概念マッピング:**

| 既存 (puzzle.ts) | Agent化後 |
|------------------|-----------|
| PuzzleCell | ReiAgent (behavior: cooperative) |
| PuzzleCell.candidates | Agent の value (𝕄{0; 候補₁, 候補₂, ...}) |
| PuzzleCell.eliminationHistory | Agent の memory |
| ConstraintGroup | Agent 間の binding (関係属性) |
| propagateStep() | Mediator の 1ラウンド |
| solvePuzzle() | Mediator.run() (収束まで) |

**セルAgent のライフサイクル（1ラウンド）:**

```
perceive: 同じ行・列・ブロックのAgent の確定値を観察
  → 「隣のAgentが3に確定した」を知覚

decide: 自分の候補から知覚した値を消去する判断
  → confidence = 消去できる候補数 / 残り候補数
  → 候補が1つになったら transform(確定) を判断

act: 候補消去 or 確定
  → 確定イベントを発火 → 他のAgentが次ラウンドで知覚
```

**Mediator の役割:**
- 全セルAgent の並行 perceive → decide → act を管理
- 競合: 2つのセルが同時に同じ値に確定しようとする → contradiction 検出
- 収束: 全セルが `none` (変化なし) を選択 → 停止判定
- 高度な推論（Naked Pair等）: 2+ラウンド目で layer (層) を上げて探索

**新しい Rei 構文:**

```rei
// 従来のパズル構文はそのまま維持（後方互換）
let p = sudoku(grid)
p |> solve

// v0.5 Agent 構文（新規追加）
let p = sudoku(grid)
p |> agent_solve              // Agent化して解く
p |> agent_solve |> 調停σ    // 解法過程の統計

// 段階的に観察
p |> agent_propagate          // 1ラウンドだけ実行
p |> agent_propagate(5)       // 5ラウンド実行
```

---

### B. ゲーム → Agent化

**概念マッピング:**

| 既存 (game.ts) | Agent化後 |
|----------------|-----------|
| Player 1 / Player 2 | ReiAgent (behavior: competitive) |
| GameState.board | 共有環境 (environment) |
| strategy ("minimax") | Agent の will (意志属性) |
| selectBestMove() | Agent.decide() の内部ロジック |
| playMove() | Agent.act() |
| moveHistory | 両Agentの memory |

**プレイヤーAgent のライフサイクル（1ターン）:**

```
perceive: 盤面を観察、相手の直近の手を知覚
  → 「相手が中央に置いた」「リーチがある」等を認識

decide: 意志(will)に基づいて手を選択
  → behavior=competitive: minimax で最善手
  → behavior=reactive: 相手のリーチを防ぐ
  → behavior=contemplative: モンテカルロシミュレーション

act: 手を打つ
  → game:move イベントを発火
  → 盤面が更新される
```

**Mediator の役割:**
- ターン管理: Player Agent を交互に活性化
- 不正手の検出: 既に埋まったマスへの着手 → cancel
- ゲーム終了判定: 勝利/引分/投了

**新しい Rei 構文:**

```rei
// 従来のゲーム構文はそのまま維持（後方互換）
let g = game("tictactoe")
g |> play(4)
g |> auto_play("minimax", "random")

// v0.5 Agent 構文（新規追加）
let g = game("tictactoe")

// 各プレイヤーを Agent として生成
g |> agent_play("competitive", "cooperative")
// → Player 1 = competitive (minimax), Player 2 = cooperative (防御重視)

// 1手ずつ観察
g |> agent_turn              // 現在のプレイヤーが1手打つ
g |> agent_turn |> 自律σ    // その手の判断理由を確認

// 自動対局 (Agent版)
g |> agent_match             // 決着まで自動実行
g |> agent_match |> 調停σ   // 対局統計
```

---

### C. 統一抽象 — AgentSpace

パズルとゲームの共通上位概念として `AgentSpace` を設計する。

```typescript
interface AgentSpace {
  reiType: 'AgentSpace';
  kind: 'puzzle' | 'game' | 'hybrid';

  // 環境
  environment: Map<string, any>;    // 共有状態（盤面等）

  // Agent 群
  agents: string[];                  // AgentRegistry 内の ID リスト

  // ルール（制約 or ゲームルール）
  rules: AgentSpaceRule[];

  // Mediator 設定
  mediatorConfig: {
    strategy: ConflictStrategy;
    maxRounds: number;
    convergenceThreshold: number;
    turnBased: boolean;              // true=ゲーム（交互）, false=パズル（同時）
  };

  // σ
  history: RoundResult[];
  solved: boolean;
  winner?: string;
}

interface AgentSpaceRule {
  type: 'constraint' | 'legality' | 'victory' | 'draw';
  check: (env: Map<string, any>, action: Decision) => boolean;
  message: string;
}
```

**統一的な Rei 構文:**

```rei
// パズルとゲームが同じパイプで動く
let p = sudoku(grid) |> as_agent_space
let g = game("tictactoe") |> as_agent_space

// どちらも同じコマンドで実行
p |> run_agents            // 協調的に収束まで実行
g |> run_agents            // 競争的に決着まで実行

// どちらも同じσで観察
p |> 調停σ
g |> 調停σ
```

---

## 実装計画

### Phase 4a: AgentSpace 基盤 (推定 +400〜500行)

新ファイル `src/lang/agent-space.ts`:

1. `AgentSpace` インターフェース定義
2. `createPuzzleAgentSpace(puzzle: PuzzleSpace)` — 既存パズルをAgent化
   - 各セルを ReiAgent として spawn
   - 制約グループを binding として登録
   - Mediator を cooperative 戦略で設定
3. `createGameAgentSpace(game: GameSpace)` — 既存ゲームをAgent化
   - 各プレイヤーを ReiAgent として spawn
   - 盤面を共有環境として設定
   - Mediator を turnBased モードで設定
4. `runAgentSpace(space: AgentSpace)` — 統一実行関数
   - puzzle: Mediator.run() (並行、収束検出)
   - game: ターンベース実行ループ

### Phase 4b: パズルAgent統合 (推定 +300行)

evaluator.ts への統合:
1. パイプコマンド追加: `agent_solve` / `自律解法`, `agent_propagate` / `自律伝播`
2. PuzzleCell → Agent の変換ロジック
3. 制約伝播の Agent ベース再実装
4. Naked Pair 等の高度推論を「層を上げる」Agent行動として実装

テスト: `tests/puzzle-agent.test.ts` (+30〜40テスト)

### Phase 4c: ゲームAgent統合 (推定 +300行)

evaluator.ts への統合:
1. パイプコマンド追加: `agent_play` / `自律対戦`, `agent_turn` / `自律手番`, `agent_match` / `自律対局`
2. Player → Agent の変換ロジック
3. Minimax を Agent.decide() 内部に移植
4. 異なる behavior の Agent 同士の対局

テスト: `tests/game-agent.test.ts` (+30〜40テスト)

### Phase 4d: 統合テスト・安定化

1. パズル ↔ ゲーム横断テスト
2. パフォーマンスプロファイリング（9×9数独 = 81 Agent 同時実行）
3. README / CHANGELOG 更新
4. npm v0.6.0 公開

---

## 後方互換性

**完全後方互換を保証する。**

既存の構文は全てそのまま動く:
```rei
sudoku(grid) |> solve           // 従来通り手続き的に解く
game("tictactoe") |> auto_play  // 従来通り関数的に対局
```

Agent 版は新しいパイプコマンドとして追加:
```rei
sudoku(grid) |> agent_solve     // Agent化して解く
game("tictactoe") |> agent_play // Agent化して対局
```

---

## 成功基準

1. 9×9 数独が Agent ベースで正しく解ける
2. 三目並べが Agent 対局で正しく動作する
3. 既存の 721 テストにリグレッションなし
4. 新規テスト 60〜80 追加
5. `agent_solve` と `solve` が同一解を生成
6. `agent_play` と `auto_play` の勝敗結果が戦略的に妥当

---

## 思想的位置づけ

D-FUMT の拡張予定領域「全パズル統一理論」「全ゲーム統一理論」が、
v0.5 の Agent 基盤によって**同一の理論**に統合される。

```
パズル = 協調的Agent群の収束問題
ゲーム = 競争的Agent群の均衡問題
```

この統一は、仏教の「縁起」の思想そのものである。
個々のセル/プレイヤーは独立した存在ではなく、
相互依存のネットワークの中で、知覚→判断→行動のサイクルを通じて、
全体の調和（パズルの解）または均衡（ゲームの結果）に至る。
